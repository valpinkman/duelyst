# Tests whose verdict is not reproducible even on a fresh database and a fresh
# Firebase emulator. The baseline gate ignores them in BOTH directions, so they
# can neither fail CI nor be credited as fixed.
#
# Everything here is debt: an ignored test guards nothing. The list exists
# because a gate that fails randomly gets ignored, and an ignored gate is worse
# than no gate. Keep it as close to empty as possible.
#
# Both entries currently PASS on almost every run -- each was observed to fail
# exactly once across roughly eight CI-equivalent runs, which is precisely the
# rate that erodes trust in a gate. Neither has been diagnosed. The chest and
# inventory suites are seeded (test/helpers/seeded_random.js), so the remaining
# source is state these tests assume but do not establish.
inventory.js > inventory module > unlockBoosterPack() > expect cards you received from the booster to be marked as NEW and UNREAD
gauntlet.js > gauntlet module > updateArenaRunWithGameOutcome() > expect 3 losses to end the run
