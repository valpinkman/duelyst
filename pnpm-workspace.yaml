packages:
  - 'packages/*'
  # shared build/lint/format configuration consumed by every package
  - 'tooling/*'
  # in-place workspace members (physical relocation deferred to the TS phase):
  - 'app/sdk'
  - 'app/common'
  # Electron shell (plan 7.4)
  - 'desktop'
  # desktop/ (Electron shell) is intentionally not a workspace member yet:
  # it has its own lockfile and packaging flow. Fold it in once apps/ exists.
