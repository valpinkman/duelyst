{
  "//": [
    "WORKING config for the CoffeeScript -> JS -> TypeScript migration.",
    "Deliberately loose: files are renamed .js -> .ts in batches and typed",
    "incrementally. tsconfig.strict.json is the DESTINATION - run",
    "`pnpm tsc:strict` to see how far the codebase still is from it.",
    "Nothing is emitted from here: Vite/rolldown builds the client, tsx runs",
    "the server, vitest runs the tests. This config is for editors and",
    "`pnpm typecheck` only."
  ],
  "compilerOptions": {
    "target": "es2020",
    "module": "preserve",
    "moduleDetection": "force",
    "moduleResolution": "bundler",
    "esModuleInterop": true,
    "allowJs": true,
    "checkJs": false,
    "strict": false,
    "noImplicitAny": false,
    "resolveJsonModule": true,
    "skipLibCheck": true,
    "forceConsistentCasingInFileNames": true,
    "noEmit": true,
    "types": ["node"]
  },
  "include": [
    "apps/client/types/*.d.ts",
    "apps/client/**/*.ts",
    "packages/sdk/**/*.ts",
    "packages/common/**/*.ts",
    "packages/data/**/*.ts",
    "packages/config/**/*.ts",
    "apps/server/**/*.ts",
    "apps/worker/**/*.ts",
    "test/**/*.ts",
    "tools/**/*.ts"
  ],
  "exclude": [
    "node_modules",
    "dist",
    "apps/client/vendor",
    "apps/client/resources",
    "apps/client/original_resources",
    "packages/chroma-js",
    "packages/Backbone.VirtualCollection",
    "apps/desktop",
    "apps/client/tools/FileSaver.min.ts",
    "apps/client/tools/dat.gui.ts"
  ]
}
